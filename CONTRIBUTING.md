<!-- MAGI -->
# 執筆ガイド

## ディレクトリ構造

各章は `chNN-short-name/` とし、次のファイルを置きます。

```text
chNN-short-name/
├── README.md          章の概要・学習目標・MCC コード
├── docs/
│   ├── index.md       AI が保守する共通本文
│   ├── 01-name.md     執筆者自身の言葉による本文・所感
│   └── assets/        画像などの関連ファイル
├── exercises/
│   └── index.md       練習問題と解答
└── assignments/
    └── index.md       提出課題
```

## 本文の役割分担

- `docs/index.md`: カリキュラムを網羅するために AI が作成・更新する共通本文です。冒頭に「このファイルは AI により保守されます」と明記します。
- `docs/01-*.md`: 人間が自身の構成・文体・経験で書く本文や所感です。冒頭に「このファイルは執筆者の言葉で書きます」と明記します。

人間が書いたファイルは AI 版の下位版ではありません。異なる説明や視点を持つ教材として並列に扱います。複数追加する場合は `02-*.md`、`03-*.md` と連番にします。

## Markdown の書式

- 章タイトルには `#`、節には `##`、項には `###` を使います。
- インライン数式は `$ ... $`、ブロック数式は `$$ ... $$` で書きます。数式は KaTeX で描画されます。
- 解答は `<details><summary>解答</summary>` と `</details>` で囲みます。
- 学習目標や章末確認には `- [ ]` のチェックリストを使います。

## 演習の書式

各問は `## 問N` で始め、解答を次の形で記述します。

```markdown
## 問1

[QUESTION]

<details><summary>解答</summary>

**選択: A / B**
**答: A**

[EXPLANATION]

</details>
```

- 選択式では、選択肢を `**選択: A / B**` の形で正答行の直前に置きます。
- 正答は `**答: ○○**` の形で 1 行にします。
- 記述式では `**選択: ...**` の行を省略します。
- `**答: ...**` より後ろは解説として扱われます。
- コロンは半角の `:`、選択肢の区切りは半角スペースを含む ` / ` を使います。

## Pull Request の手順

1. リポジトリを fork し、作業用ブランチを作成します。
2. 変更後に見出し、リンク、数式、演習の書式を確認します。
3. 変更内容と確認方法を Pull Request の本文に記載します。
4. レビューを受け、必要な修正を追加コミットします。

執筆者名は Git のコミット履歴と Pull Request の履歴に残ります。共同執筆者を明示する場合は、コミットメッセージ末尾に GitHub が認識できる `Co-authored-by: Name <email>` を記載してください。
<!-- /MAGI -->
